{
  "name": "apollo-federation",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^4.0.0",
    "lerna-changelog": "^1.0.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:services": "lerna run start --parallel --ignore gateway",
    "start:gateway": "lerna run start --scope gateway"
  },
  "private": true
}
